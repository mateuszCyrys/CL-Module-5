/*let print = function(number) {
    console.log(number);
}

let randomize = function(param1, param2, callback) {
    if(typeof callback != 'function') {
        throw Error('callback param is not a function');
    }
    let randomNumber = Math.round((Math.random() * (param2-param1)) + param1 );
    callback(randomNumber)
}

 */